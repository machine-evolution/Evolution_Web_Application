package evolution.pattern.singleton;

public class Moon {
    // Define a static field holding the Moon object.

    // Define a private constructor.

    // Create a static method that returns the Moon object (hunger)

    // Create a static method that returns the Moon object (full)
}
